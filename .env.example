# PostgreSQL Connection (Optional: When using Azure Database for PostgreSQL or External PostgreSQL)
# Format: postgres://username:password@hostname:port/database?sslmode=require
# Important: URL-encode the password if it contains special characters like @, :, /, ?, #, etc.
# POSTGRES_URL=

# For Docker Compose (When using PostgreSQL container)
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# Azure OpenAI (if needed)
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT_NAME=

# Image Search API
# Unsplash API
# Get your free API key at: https://unsplash.com/developers
UNSPLASH_ACCESS_KEY=
